class peepBoard(object):
    """ Handles information about the peep that needs to be passed about,
        Mainly needs. """


    def __init__(s, currentLeaf, needlist=None):
        s.needlist = needlist
        s.currentLeaf = currentLeaf
